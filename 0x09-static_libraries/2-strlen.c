#include "main.h"

/**
 * _strlen - checks the length of a string
 * @s: a character in a string
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
